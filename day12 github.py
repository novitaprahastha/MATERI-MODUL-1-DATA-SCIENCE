#GITHUB

# print('welcome')
# x = 12
# print(x+x)

# staging area => add . (atau ketik nama file nya)
# commit => commit
#untuk hapus yang ga jadi di staging area : git rm --cached nama file
#cek ada berapa commit: git log (semua nya)
                        # git log --oneline (keluar intinya)
#buat commit : git commit -m "komentar/keterangan"
#untuk liat liat commit sebelumnya tanpa mengubah isinya => git checkout (nomor commit id)
#untuk kembali ke commit awal => git checkout master
#git status => untuk cek status 

#untuk menghapus commit yang sebelumnya: git reset (nomor commit id)
                                        #git reset (nomor commit id) --hard
#untuk bikin branch: git branch (nama projek)
#untuk lihat ada branch apa apa (git branch -a)
#untuk menghapus branch => git branch -D 
#untuk switch branch lain => git checkout (nama branch)
#untuk gabungin branch ke master => git merge (nama branch nya)
#untuk bikin branch lgsung bekerja di branch tersebut => git checkout -b (nama branch)
# print(x/2)